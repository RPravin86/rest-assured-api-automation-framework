# GoREST exposes one public host. This separate file demonstrates environment switching
# and can point to a dedicated environment when one becomes available.
base.url=https://gorest.co.in
base.path=/public/v2
connection.timeout.ms=10000
response.timeout.ms=15000
